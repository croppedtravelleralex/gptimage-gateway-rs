# Changelog — gptimage-gateway-rs

## Unreleased

+ [决议] 2026-07-27：**§6.3 六项架构决策闭合**（以 Panda 现网为准）→ [docs/28-decisions-20260727.md](docs/28-decisions-20260727.md)。双轨**合流**；`ticket_pool`/`control_client` 移出 workspace；鉴权 R2 目标 = `:8012` API key、`:8013` 短期对齐现 Panda `AUTH_DISABLE=1`；异步图片队列与 CPA 端点纳入范围。`ticket_pool`/`control_client` 源码保留备查。
+ [提交] 2026-07-27：**P0 工程基线首次入库** — auth/UI/CI/fixtures/`web/`/`docs/21`–`28`、安全修复、orphan crate 冻结；`cargo test --workspace` 绿（不含 frozen crate）。
+ [实测] 2026-07-26：**Phase B′ 判据 1 通过 —— `wreq` 能复现 curl_cffi 的 TLS 指纹** → 新增 [docs/27-tls-fingerprint-spike-20260726.md](docs/27-tls-fingerprint-spike-20260726.md)，spike 在 `spike/tls-fingerprint/`（独立 cargo 项目，**不进主 workspace**，避免 BoringSSL 编译拖慢主 CI）。实测对照：**JA3N 逐字节相同**（chrome124 `4c9ce260…`、chrome131 `dee19b85…`）、**JA4 相同**（`t13d1516h2_8daaf6152771_02713d6af862`）、**JA4_r 逐项对齐**（15 cipher + 16 扩展 + 8 签名算法）、**Akamai HTTP/2 指纹相同**（`52d84b11…`，SETTINGS/窗口/伪头序 `m,a,s,p` 全对齐）。**JA3（未排序）两侧不同，但这不构成失败** —— 真实 Chrome 自 2023 起对扩展顺序随机化，curl_cffi 自己三个 profile 的 JA3 也各不相同而 JA4 相同，CF 用的是排序后指纹，**拿 JA3 判等价是错误判据**。两个必须记录的约束：① **chrome120 不稳定**，连续两轮 JA4 从 `t13d1516h2` 变 `t13d1517h2`（扩展数 16→17），chrome124/131 完全稳定 —— 迁移应弃用 chrome120（它在生产 `FP_PROFILES` 里出现 2 次）；② **必须 `http2(true)`**，设 false 会让 Akamai 指纹变 `787b7899…` 不匹配。构建成本不可忽略：BoringSSL 源码编译首次约 10–15 分钟，需 `cmake` + **`libclang`**（本机原本没有，用 `pip download libclang` 取 wheel 内的 DLL 解决）。**硬阻塞性质从「技术未知」正式降级为「工作量」**，[docs/26](docs/26-perf-measured-20260726.md) §8 的完全重写收益预估前提成立。但**判据 1 通过 ≠ 能过 CF** —— 判据 2（CF 通过率）才是业务验收，仍未测。

+ [修复] 2026-07-26：**P0 阻断全部解除，五道门禁首次全绿**。`cargo build --workspace` ✅ / `cargo test --workspace` ✅ **56 passed**（原先 0 个能跑）/ `cargo fmt --check` ✅ / `cargo clippy -D warnings` ✅ **0 warning** / `check_runlog_desense.py` ✅ **真实 DESENSE_OK**（清理前实测报 14 处命中，不再是恒绿的虚假门禁）。① **`ticket_pool` 4×E0277** —— 没走「给 `uuid` 加 serde feature」那条路，而是**删掉 `SentinelTicket` 的 `Serialize`/`Deserialize` derive**（纯内存池不需要序列化）。一个动作解三件事：E0277 消失、堵住「一次 `{:?}` 泄全池 4 个明文 token」的 CRITICAL（改手写 `Debug` redact）、并修正**语义反了的守卫**（原 `acquire()` 只在 `PerCallFinalize`「每次调用都 finalize」即禁止复用的策略下放行，与命名互相否定 → 改 `ReusePolicy::allows_pooled_acquire()`）。9 个测试含两个 redaction 断言。② **CORS 构造 panic** —— 新增 `cors_layer_from()` 读 `GATEWAY_CORS_ORIGINS`。⚠️ 过程中发现 tower-http 对 origin / **methods** / **headers** 三处都单独 assert 与 credentials 的冲突，首版只改了 origin，**被自己写的测试抓出来** —— 若无测试，这个修复会在任何人设置 allowlist 的那一刻 panic。③ **bringup 默认值反转** —— 默认 `127.0.0.1:8013` + `AUTH_DISABLE=0` + `IMAGE_ENABLED=0`，并**主动拒绝**「`AUTH_DISABLE=1` + 非 loopback」组合（exit 2），缺密钥时 fail-fast 并给出 `openssl rand -hex 32` 命令。④ **建 CI** —— `.github/workflows/ci.yml`（rust / desense / web 三 job），这是前两条能长期活在 `develop` 的根因。

+ [安全] 2026-07-26：**7 项 CRITICAL 全部修掉**。① 默认 admin 口令（`auth/src/lib.rs` `bootstrap_admin_if_empty`）→ `bail!` + 12 字节口令下限；② 默认 JWT secret（36 字节硬编码 fallback **恰好绕过** 32 字节校验）→ `bail!`；③ **禁用用户不生效** → `require_auth` 验签后**回查 DB**，`disabled` 返 403、用户不存在返 401，并用 DB 的 role 覆盖 token claim —— **角色降级因此也即时生效，不必等 token 过期**；④ `X-Preferred-Account-Email` 无授权校验 → `resolve_account` 加 `is_admin` 参数，member 传该头直接 403；同时修掉「未知邮箱静默造 `access_token: ""` 空账号继续走」的 fail-open（改 404）；⑤ helper `/v1/internal/*` 无鉴权吐 100 个明文 token + 含口令代理串 → `X-Helper-Token` 共享密钥（`hmac.compare_digest`），**未设密钥时 fail closed 返 503**，candidates 响应剥离 `access_token`（→ `has_token: bool`）与代理口令（→ `proxy_host`）；⑥ `openai_face.py` 绑 `0.0.0.0` 且 7 路由无鉴权 → 默认 loopback + 同款鉴权，`/health` 的 `pin_email` → `pin_loaded: bool`；⑦ 每请求改写模块级全局 config（40 线程池内互相覆盖，且 `_save()` 可**持久化进生产 `config.json`**）→ 改类描述符 override，不碰 `config.data`，`_save()` 无可持久化内容。

+ [安全] 2026-07-26：**HIGH 项**。`/health` 不再泄露 `pin_email` —— 敏感字段移至新增的 admin-only `GET /api/admin/status`（有测试断言 `/health` 响应体不含 pool 地址）；登录响应体不再回显 JWT（只走 HttpOnly cookie，有测试断言 body 无 `token` 字段）；`run_image` 每次新建 `reqwest::Client` → `HelperClient` 持有复用的 `image_http`，顺带修掉 SSE 流被 120s 客户端硬切；helper 侧 `_ACCOUNT_CACHE` 加 60s TTL、`_QUOTA_CACHE` 加 per-key single-flight 锁、3 处 `traceback` 出响应体改 `logging.exception` + `error_ref`、调用方 `proxy` 默认忽略（需 `HELPER_ALLOW_CALLER_PROXY=1`）、`model` 如实回显。

+ [修复] 2026-07-26：**`self=0` 红线现在可信了**。`error_class.rs` 的 `client` 分支此前**不可达** —— `fault.unwrap_or("upstream")` 使 `fault=None` 默认落 upstream，`f == "client"` 永不命中，客户端输入引发的错误一律记 `upstream`，指标有系统性偏置。改为：显式 `fault` 优先 match，无 fault 才走 message 启发式，最后才 fallback。8 个测试含 `client_fault_is_reachable` 与 `self_fault_wins_over_upstream_message`。helper 侧 `_classify_fault` 同样补了 `client` 分支。

+ [修复] 2026-07-26：**契约漂移修了 1 项（17 项中最要紧的）** —— `image_contract.rs` 三处**固定 UUID 字面量**（`"fixture-user-message-id"` / `"fixture-partial-query-id"`）改为 `new_uuid()`。原来每个请求的 message id 都相同，上游会当作同一 turn 的重放。讽刺的是 `new_uuid()` 此前是 dead code —— 它正是修这个问题所需的函数。新增 `message_ids_are_unique_per_build` 测试。**剩余 16 项漂移未修。**

+ [清理] 2026-07-26：**删 19 个死脚本**（`scripts/` 从 25 个减到 6 个），其中 7 个含真实账号邮箱。`_tmp_deploy_rust_ticket_round.py` 是**用 `scp` 往 panda 推代码**的，违反部署铁律，**直接删而非转正**。`_tmp_rust_ticket_image_round.py` 转正为 `scripts/rust_image_round.py`：邮箱改环境变量、加 `GATEWAY_TOKEN` 鉴权头、runlog 里的邮箱做 mask、docstring 声明「只读诊断」。`bin/gptimage-gateway-rs`（7.2MB 未 strip ELF，`strings` 泄露构建主机路径）已 `git rm --cached`。`.gitignore` 补 `.env.*`（保留 `!.env.example`）/ `config.json` / `*.db`·`*.sqlite` / `*.key`·`id_rsa*` / `bin/` / `spike/` 产物。新增 `.desense-allow` 白名单机制。

+ [文档] 2026-07-26 晚：**`docs/21` 按代码逐条订正（11 项）** —— 该文原是全仓与实际偏离最大的一份。要点：① 顶部加未部署警告 + panda 全 404 实测；② 「`develop` 是集成分支、鉴权+UI+契约已合并」是错的，`main..develop` = **0 提交**、无 feature 分支、`git ls-files web` = **0**；③ 补记 `auth/lib.rs:166-173` 在 bootstrap 变量缺失时**静默回退** `("admin","admin-change-me")` 建库（CRITICAL）；④ `/v1/images/edits` 三点纠正 —— 错误码是 `image_edits_deferred` 不是 `image_deferred`、`main.rs:532-542` 把 State 解构为 `_st` **完全不读 `image_enabled`**（即使 `=1` 也返 501）、生产态该路由实测 **404**；⑤ 生图路由并回「成员+管理员」（原单列易读成不需登录，实际 `main.rs:108-114` 同挂 `require_member`）；⑥ `/ops` `/logs` 标「占位」（零 API 调用）并补漏掉的 `/register` 页；⑦ dev 流程加前置警告 —— 跨域带 cookie 撞上 `allow_origin(Any)`+`allow_credentials(true)` **启动即 panic**，现网没崩只因二进制里没这段代码，故恢复顺序上**修 CORS 必须排在部署之前**；⑧ `GATEWAY_STATIC_DIR` 补记 `.filter(|p| p.is_dir())` 的**静默降级**失败模式（仅 `/health.static_ui` 可观测）；⑨ 4 个虚假 `[x]` 全部取消勾选（`cargo build --workspace` FAIL，19 测试跑不了；403 分支在 `AUTH_DISABLE=1` 下永不触发）；⑩ 环境变量表拆三张并补 **10 个漏项**，修正 `AUTH_JWT_SECRET`「必填」失真（有 33 字节硬编码 fallback 恰好通过 `len()>=32`），点明 `PIN_ACCOUNT_FILE`/`_JSON` 是**全仓唯一真正必填**（二者全缺则 `bail!`）。
+ [文档] 2026-07-26 晚：**修正一处自身证据链错误** —— 早前用「`:8013/me` 404」证明 auth 未部署是**无效论证**：真实路由是 `/api/auth/me`（`main.rs:99` 的 `.nest("/api/auth", ...)` 之下），`/me` 从未在任何版本注册过，即使全部部署也仍是 404。已改用真实路径重测：`/api/auth/me`、`/api/auth/login`、`/api/admin/users`、`/api/backend/capabilities`、`/v1/images/edits` **全部 404** —— 结论不变，依据已换成有效的。`docs/17` / `docs/25` / `CHANGELOG` 三处表述同步订正。
+ [文档] 2026-07-26 晚：**旧数字残留清理** —— `image_schedule_core` 585→**597**（原式 `585+510=1,107` 等号本身不成立）；路径 A 合计 1,095→**1,107**（`docs/24` 两处）；契约漂移 ≥7→**17**（`plan.md` 三处、`SOURCE.md`、`docs/22` 两处）；`_tmp_*.py` 「18 个已进 git」→**磁盘 17 / 跟踪 15**，含邮箱脚本「7 个全被跟踪」→**5 个已跟踪 + 2 个未跟踪**（后者恰是建议转正的，转正前必须先清），`CHANGELOG.md:21`→**`:30`**；可删脚本 18→**19**（25 = 保留 4 + 转正 2 + 可删 19）；端点「129 vs 16」→**129 vs 生产态 7 / 工作树 15**。CHANGELOG 中作为历史修正记录引用的旧值按原则保留。
+ [实测] 2026-07-26 晚：**panda 生产现采对照** → 新增 [docs/25-panda-vs-rust-20260726.md](docs/25-panda-vs-rust-20260726.md)（代码/文档/运行时三维）。三条头条：① **本地 2,707 行 Rust 中只有 943 行进了 git**，其余 1,764 行（全部 `auth`/`ticket_pool`/`control_client`/`error_class`/`image_contract`）是未跟踪工作树，**外加整个 `web/`**（`git ls-files web` = 0 条）、`fixtures/protocol/` 8 份、`docs/21`–`26` 7 份；panda HEAD `6509fba` 与本地 HEAD 源码逐字节相同，`git rev-list --count main..develop` = 0（两分支无差异、无 feature 分支），本地只领先两个纯文档 commit —— 按部署铁律的 git 链路这些代码**当前不可能到达 panda**，且一旦提交又过不了 `cargo build`；`web/` 更因 `.gitignore` 忽略 `out/` 而使静态 UI 产物永远送不到 panda，`GATEWAY_STATIC_DIR` 无从指向；② **文档描述的系统 ≠ 运行的系统** —— `/api/auth/me`、`/api/auth/login`、`/api/admin/users`、`/api/backend/capabilities`、`/v1/images/edits` 实测**全部 404**（注意路由前缀是 `/api/auth/me` 而非 `/me`，后者从未注册过，不可作证据），运行中 ELF 的 `strings` 无任何 auth 符号，`docs/21`(80 行) + `docs/17` 相关段共约 137 行描述的是一个**从未部署过的版本**；③ 运行二进制溯源到 commit `7c34159`（2026-07-21 10:31 编），`sha256sum /proc/<pid>/exe` 与磁盘一致。另：端点覆盖 **7 / 129**（生产态），本地工作树 15；helper 与生产**同一镜像**且把 `api`/`services`/`utils` ro 挂进 `/app` 再 `sys.path.insert` —— **Rust 的数据面不是移植副本，是通过 sys.path 反向调用同一份 Python 生产代码**，二进制无法脱离 Python 树独立部署，且两容器 rw 共享同一 `data/`。
+ [实测] 2026-07-26 晚：**性能基线现采 + 收益重估** → 新增 [docs/26-perf-measured-20260726.md](docs/26-perf-measured-20260726.md)，**[docs/13](docs/13-perf-baseline-compare.md) 的四行 Rust 预估全部作废**。① `docs/13` 记 Python「空闲 CPU 0.5–0.7%」，**实测 98.5%**，偏两个数量级 —— 以它为基线的所有推导失效（Mem 224MB 与耗时 p50 6.32s/p95 12.05s 两项吻合）；② **98.5% 已精确归因**：29 线程中 10 个各占 9.0–11.5%（几乎完全均分、全 `futex_wait_queue`、启动时刻一致），精确匹配 `config.json` 的 `image_task_queue.submit_workers=10`（`image_task_service.py:1059/1199`），是 condition-wait 唤醒 × 持锁扫描 × GIL 在 2 vCPU 上的叠加；③ **Rust 进程 3 天累计只用 0.33 秒 CPU、私有内存仅 432KB** —— 其「低资源」不是重写效率的证明，是空闲进程的证明；④ **当前架构下开销下降 = 0%，系统总 RSS 反而 +5.2MB** —— Rust 出站 6 个目标全指向本地 helper、零个指向 ChatGPT，SSE 是纯字节透传（`main.rs:342`），1.17MB b64 只做长度检查（`main.rs:492`），PoW/turnstile 全在 Python；⑤ 并发上限实测确认为 anyio `total_tokens`=**40** × `_lock_for_email` 同账号串行 × GIL 单核，Rust `Semaphore(3)` 比三者都松、**不是瓶颈**；⑥ 完全重写（Python 归零）后可辩护区间：RSS **−82%~−93%**、稳态 CPU **−85%~−95%**、上下文切换 **−99%+**（709,436 vs 27）、镜像 **−96%**（960MB→25–40MB）、并发 **×5~15**、**E2E 延迟 0%**（43–111s 全是上游等待，Amdahl 封顶）。最大风险仍是 TLS 指纹：当前 runlog Rust 侧 0/6 + 0/4 全挂 CF403。
+ [修正] 2026-07-26 晚：**进度口径补两列，暴露供应链缺口** → [docs/23](docs/23-rewrite-progress.md) §1 从四口径扩为六口径。新增「**已进 git ≈2.9%**」（667 行）与「**已部署可运行 ≈5.4%**」（1,255 行）。**后者大于前者不是笔误 —— 生产上跑着 598 行没有进任何 git 仓的 Rust**：路径 A 的 `dispatch_gate.rs`/`lease_pool.rs`/`sediment.rs`/整个 `image_schedule_trace/` 在 `../gptimage` 里全是 `??`（1,107 行只有 509 行入库），而 `slot_ledger.py:268-277` 的 `SlotLedgerFacade` 检测到 `native/*.so` 即切 rust 后端 —— **这些未入库代码正在生产路径上执行**。更进一步：`libimage_schedule_core.so` 已 ro 挂载进生产容器并被调用，但 **panda 上根本没有它的源码**，无法从本机溯源到任何 commit；且 panda 的 `crates/image_schedule_trace/target/` 有 debug+release+交叉三套产物，**该 crate 曾在 panda 上编译过**（铁律一历史违规，已记录未触碰）。
+ [文档] 2026-07-26 晚：**本地「生产快照」不完整** —— `../gptimage-panda/docs/` 只有 5 份，**漏了 panda 上最新的 4 份**（`04-improvement-backlog` 481 行、`28-scheduling-queue-slot-audit` 422 行、`27-pipeline-watchdog` 90 行、`README`），恰是唯一 4 份带当期审计结论的。共有的 5 份内容零漂移。基于该快照的 `docs/23`/`24` **分母的文档部分口径待复核**（代码部分已逐文件核对无误）。反向：panda 的 `docs/README.md` 从 Windows 同步过去后，索引里 12 个目标文件/目录在 panda 上**全部不存在**，导航整体失效。另新增矛盾项：`13` 的 CPU 基线、panda README 索引失效、`27` 被 `28` 推翻 —— 合计 9 条，见 [docs/25](docs/25-panda-vs-rust-20260726.md) §2.4。
+ [文档] 2026-07-26 晚：`docs/13` 改为「作废记录 + 指向 26」；`docs/17` 按实测改写（拓扑改为 7 端点无鉴权无 UI、补 helper 共享镜像与 sys.path 反挂事实、故障树补 404/`image_edits_deferred`/脱敏虚假门禁三条、补「让文档变成真的」前置条件）；`docs/README.md` 补 25/26 并给出**冲突时优先级：25/26（生产现采）> 22/23/24（本地分析）> 其余**。
+ [计划] 2026-07-26：**行动序列与待决策清单落文档** → `plan.md` §6.0 / §6.3 + `HANDOFF.md`。P0 第一步改为**摘掉 `ticket_pool` 的 workspace member**（而非给 `uuid` 加 `serde` feature）—— 一个动作两个收益：4 个 E0277 全在该 crate，摘掉后 5 crate 全绿 / 19 测试可跑，同时停止与路径 A 已上生产的 `pre_ticket_pool.py`（重叠 80%）重复造轮子；加 feature 只是让一个零引用、语义自相矛盾的孤儿 crate 编译通过，把技术债固化进构建图。第二步为双轨决策（阻塞 Phase C 的选号/admission 设计，倾向合流）。§6.3 列出 6 项待决策：双轨处置 / `ticket_pool` 去留 / `control_client` 去留（其目标端点 `/api/accounts/admission` 在生产 `api/` 下**根本不存在**）/ 鉴权模型（API key vs JWT 语义互斥，R2 切流前必须定）/ 异步图片队列是否在范围内（2,456 行，原列非目标但实为数据面）/ CPA 7 端点归属待确认。
+ [结构] 2026-07-26：**发现两条互不知晓的 Rust 化路径** → `plan.md` §1.0′ + [docs/23](docs/23-rewrite-progress.md) §0。路径 A = `../gptimage/crates/`（`image_schedule_core` 597 + `image_schedule_trace` 510 = 1,107 行；双槽账本 account+sS / 调度门 / 租约池 / sediment 抽取 / 21 事件调度 trace），已编译 `native/*.so` 并**部署到 panda 生产**（2026-07-25），经 ctypes 被 `image_pipeline/slot_ledger.py` 的 `SlotLedgerFacade` 调用；路径 B = 本仓 2,707 行，编译失败未部署。本仓对 A **零引用**，而 `crates/ticket_pool`（285 行、编译失败、孤儿）与 A 侧已上生产的 `pre_ticket_pool.py` **重叠 ~80%**，在重造已造好的轮子。关键机会：A 的两个 crate 声明了 `crate-type = ["cdylib","rlib"]`，本仓可直接 `path` 依赖复用（`SlotLedger` 双槽+TTL watchdog、`DispatchGate`、`SedimentParser`、整个阶段耗时模型），**完全绕开 FFI 层**。三个处置选项（合流/各行其道/收编）见 docs/23 §4；决策前 `ticket_pool` 应冻结。
+ [盘点] 2026-07-26：**生产 vs Rust 能力 gap 全量清单** → 新增 [docs/24-gap-inventory.md](docs/24-gap-inventory.md)（4 并行 agent：路由/数据面/调度/运维）。① 端点 129 vs 生产态 7 / 工作树 15：已实现 0、部分 10、缺失 65、永久非目标 54；② 上游数据面 42 条能力，Rust 已接线仅 1 条（`classify_fault`）；③ **SSE 事件覆盖 0/20**；④ **重试/退避策略生产 13 条，Rust 全树 0 处**；⑤ 配置 71 键可热更新 vs Rust 18 个 env 需重启，语义重合仅 1 项；⑥ 持久化实体 20+ vs 1（`users` 表），`crates/` 内 `File::create|fs::write|OpenOptions` 零命中；⑦ 后台循环 9 vs 0。两个结构性不兼容：**鉴权语义互斥**（生产 API key vs 本仓 JWT，同一个 `Authorization: Bearer`，存量 OpenAI 客户端换端口即 401，靠 `AUTH_DISABLE=1` 掩盖至今）、**`error.type` 恒为 `gateway_error`**（OpenAI SDK 对 `rate_limit_error` 的特化重试失效，且 429 不带 `Retry-After`）。R2 切流前必须补齐的 10 项阻断级运维能力已单列。
+ [修正] 2026-07-26：**进度数字分子分母双错，已重算**。分子漏了路径 A 的 1,107 行；分母漏了 3,374 行调度/队列（最大一处：把 `image_task_service.py` 2,456 行当成「UI 任务队列」排除，实为异步图片队列 + 槽位释放，是数据面；另有 `humanlike_scheduler` 350、`account_workload_policy*` 368、`image_sync_adapter` 147、`text_task_queue` 53）。修正后：分母 23,256、分子 1,933、**代码体量 8.3%**（原 4.2%）、**已生产可运行 4.8%**（原 1.2%，且全部来自路径 A）、**功能加权 12.8%**（原 12.5%）。加权数几乎不变是巧合——新增的 Rust（双槽 80% + trace 100%）恰被扩大的分母（队列/调度全 0%）抵消，**头条数字稳定但构成全错**。另修正 `docs/22` §3：契约漂移实际 **17 处**而非 ≥7 处（含 prepare body 固定 UUID 字面量、start body 缺 `@Create image` mention 前缀导致 turn 不进 picture_v2、文本体缺 11 键多 3 键）。
+ [决议] 2026-07-26：**Phase B′ 立项 —— 做，不砍目标**。选型 `wreq`（★944，2026-07-20 活跃；`rquest` 继任者，原仓已改名 `rquest-deprecated` 停更 2025-01）；备选 `impit`（★555）/ `primp`（★565）；不自研 rustls 指纹。四条出门判据：JA3/JA4 与 curl_cffi 实测一致 → CF 通过率不劣于 Python 基线 → SSE 长连接跑通一轮完整生图 → `self=0`。失败则把「数据面重写」从 `plan.md` §0 目标删除，本项目正式改定位为 face + 鉴权层，`docs/13` 并发收益预估同步作废。硬阻塞性质因此从「技术未知」降为「工作量 + 实测验证」。
+ [来源] 2026-07-26：**拉取生产快照** `../gptimage-panda`（`ssh panda:/root/gptimage`，53M / 279 py，排除 data/backups/web_dist/target/.git）作为 Rust 重写的对照真相源；`SOURCE.md` 改以它为准。两树逐文件核对（`services/`+`api/`+`utils/` 共 118 个 py，归一化行尾）：**115 个内容相同**，2 个不同（`image_pipeline/guards.py`、`yumail_otp.py`），1 个仅生产有（`register/domain_intel.py`，永久非目标）。早先 `chatgpt_web_request.py` / `proxy_service.py` 的 md5 差异**纯属 CRLF↔LF**，内容逐字节相同 → **`docs/22` §3 的契约漂移判定对生产同样成立**。唯一数据面实质差异：生产**未收到** conc10 修复，`guards.py` 仍用 `dispatchable_candidate_count`，会在 inflight 饱和时误报 `image pool starved`。⚠️ 该目录含真实凭据（`config.json` 的 `auth-key`、`proxy_list.json` 内联代理口令），不在任何 git 仓内，须保持。
+ [审计] 2026-07-26：**全量只读审计**（6 并行 agent：架构/Rust/安全/Python/构建/文档）→ 新增 [docs/22-audit-2026-07-26.md](docs/22-audit-2026-07-26.md)。要点：① `cargo build --workspace` **失败**（`ticket_pool` 的 `uuid` 缺 serde feature）+ `CorsLayer` `Any`×`allow_credentials` 必 panic + 无 CI —— 本波新代码从未真正跑起来；② 5 项安全 CRITICAL（后续补入 PY-1/PY-2，现行口径 7 条）（默认 admin 口令 / helper `/v1/internal/accounts/candidates` 无鉴权吐 100 个明文 token / `openai_face` `0.0.0.0` 无鉴权 / `X-Preferred-Account-Email` 无授权校验 / 部署脚本 root `extractall` RCE），叠加 bringup 默认 `0.0.0.0`+`AUTH_DISABLE=1`；③「禁用用户」控制**实际不生效**（`require_auth` 只验签不查库，无吊销）；④ `check_runlog_desense.py` 扫描目录写死在被 gitignore 清空的路径 → **恒绿的虚假门禁**；⑤ helper SSE `conversation.done` 把累计全文当增量发 → 客户端收到重复全文；⑥ helper 每请求改写 gptimage 全局 config 单例，可污染生产 `config.json`。未发现真实 bearer/cookie/API key 硬编码；5 个 `.b64` 从未入 git 历史。
+ [文档] 2026-07-26：**修正进度虚报** → 新增 [docs/23-rewrite-progress.md](docs/23-rewrite-progress.md)。数据面重写实际 **≈12%**（功能加权）/ 4%（代码体量）/ 0%（上游字节数 —— Rust 至今未向 ChatGPT 发出过一个字节）。Phase 检查表真 ✅ **2/9**，原文档声称 4/9。降级项：Phase A+ ✅→⚠️（代码在但 bringup 不注入 `AUTH_*`/`GATEWAY_STATIC_DIR`，Panda 上无鉴权无 UI）；Phase B 契约 ✅→❌（夹具从 Rust 输出反向生成的自证，与 `chatgpt_web_request.py` 漂移 ≥7 字段，Python 注释明确这些差异会改变上游行为）；票池 ✅→⛔（编译失败 + 孤儿 + 复用策略守卫语义反了）。路线图新增 **Phase B′ TLS 指纹等价性验证** —— 原 A→E 里没有任何 Phase 负责把数据面搬进 Rust，Phase E 悬空。
+ [待办] 2026-07-26：`self=0` 红线**存在系统性偏置** —— `error_class.rs:34` 的 `client` 分支不可达（`fault=None` 默认落 `upstream`）+ helper `_classify_fault` 只产 `self`/`upstream`。另：2026-07-21 那条「纠正 CF 归因」的根因修复被随后的「helper 默认直连」回滚掉了（sticky 取号失败的原始 bug 未修），矩阵里归为 `upstream` 的 CF403 有多少是 slot-bypass 自伤，目前无法证伪。
+ [验证] 2026-07-23：**R1 开票+生图单轮**——`IMAGE_ENABLED=1` + helper；Panda `:8013` **200** @ 29.6s（`qaflow` / b64≈1.17MB）；证据 `data/runlogs/rust-ticket-verify-20260723/`。bringup：`gptimage/data` rw 挂载 + helper health 重试。
+ [功能] 2026-07-22：鉴权 `crates/auth`（SQLite + argon2 + JWT）+ gateway 角色中间件；`web/` Next 16 dashboard（登录/对话/管理）；`GET /api/backend/capabilities`。
+ [功能] 2026-07-22：Phase B 契约——`fixtures/protocol` 全量；`protocol::image_contract`；`error_class`；text SSE 透传；`control_client` admission 桩。
+ [策略] 2026-07-22：生图运行时默认关闭（`IMAGE_ENABLED=0`）；`/v1/images/*` 返回 501 deferred；UI `/image` 占位，待后端管线接入。
+ [文档] 2026-07-22：新增 `docs/21-auth-and-ui.md`；`develop` 集成分支。
+ [文档] 2026-07-22：同步 Rust Phase A 进度与 Phase A→E 重写路线——`plan.md` / `README` / `HANDOFF` / `docs/17` / `docs/18`；CF403 明确归号池/egress（gptimage `17`），不阻塞接线认定、阻塞 MVP 正式签字。
+ [功能] 2026-07-21：Rust face 接管 `:8013`——`gptimage-gateway-rs` 编排 OpenAI HTTP + `IMAGE_GLOBAL_CONCURRENCY` 信号量；Python `protocol_bridge` 仅侧车 `:19001`（curl_cffi/PoW/SSE）。目标：1/3 并发文生图单张 40–60s。
+ [修复] 2026-07-21：helper 默认 `make_backend` 直连（`MVP_FORCE_POOL_STICKY` 才走号池 sticky）；避免独立进程 `get_available_access_token` sticky 失败后落到空 ready（`no available image quota`）。
+ [功能] 2026-07-21：helper `GET /v1/internal/accounts/candidates`；Rust `X-Preferred-Account-Email` + 启动时种子号池；`scripts/panda_bringup_rust_face.sh` / `mvp_rust_conc_matrix.py`。
+ [部署] 2026-07-21：私有仓 `croppedtravelleralex/gptimage-gateway-rs`；linux amd64 产物 `bin/gptimage-gateway-rs`；panda `/root/gptimage-gateway-rs` git pull + bringup（生产 `:8012` 不动）。
+ [修复] 2026-07-21：纠正 CF 归因——对照生产后，MVP 自研问题是绕过 `get_available_access_token` 槽位 + 生图强制 `_bootstrap()`；已改为 `pool_sticky` 取号并 `_ensure_bootstrap()`。A/B 当时生产同步生图同样出现 CF/挂起，并非「多 IP 并发必然 CF」。
+ [功能] 2026-07-21：`:8013` 多号——`X-Preferred-Account-Email` 从号池解析（独立 proxy）；`GET /v1/accounts/candidates`；同号锁、异号可并行。单 pin 默认路径不变。
+ [验证] 2026-07-21：多号并发=2——独立出口已生效；健康号成功 ~36–38s；同波次另一路常 CF403 / wall_timeout。全量 4 号并发曾 0/4（上游 CF）。`self=0`。
+ [验证] 2026-07-21：并发=2 矩阵（同 pin）——第1轮 2/4（成功 ~31s，失败 CF403）；第2轮 stagger=3s 仍 1/4（成功 44s，其余 CF403）；`self=0`。结论：同代理并发易触发上游 CF，不作为出门 blocker，并发需多号/多出口。
+ [修复] 2026-07-20：串行挂死 RCA——`post_ready=null` 时客户端 75s 超时后服务端 SSE 可僵死 ~1772s；补 `post_ready=50` + `cancel_event` 墙钟 70s + 重启清僵尸。串行 2/2：45.5s / 50.9s（额度 21→19）。
+ [修复] 2026-07-20：生图 SSE 见到 `file_id` 即结束（`complete_predicate`），避免等 EOF ~90s；MVP 默认 `post_ready=null`+`poll≤25`+客户端 75s。单图实测 ~63s / `self=0`（额度先读）。
+ [验证] 2026-07-20：Panda `:8013` 1+1 矩阵绿——`qaf***@proton.me` text 6.7s / image 99.6s / remaining=23 / `self=0` / `pass_gate=true`（`rust-mvp-fixed-20260720-220907.json`）。客户端超时压到 ~100s，禁止 600–720s 硬等。
+ [修复] 2026-07-20：face 层额度检查后 `skip_quota_gate`，避免生图前双次 `get_user_info`；panda `image_sse_post_ready_timeout_secs=null`（池身份稳定后再议安全阀）。
+ [功能] 2026-07-20：MVP 额度门禁——`/v1/quota`/`/v1/quota/refresh` 走 `get_user_info` 实时刷新；生图前 `remaining>=MVP_MIN_IMAGE_QUOTA` 才继续，否则 `429 fault=quota`；matrix 不足额跳过生图。
+ [修复] 2026-07-20：生图 SSE `iter_lines` 补齐 `post_ready` 安全阀；MVP poll 默认压到数十秒，超时明确 502。
+ [修复] 2026-07-20：MVP `make_backend` 优先用账号池完整身份（fp/proxy）；pin 裸注入会导致 SSE 挂死、`tool_invoked=null`。池身份后单图约 41s 成功。
+ [文档] 2026-07-20：对照刷新——Python 基线文本 3/4、生图 3/4（self=0）；见 gptimage `docs/13` 与 Panda `rust-baseline-retry-20260720-173225.json`。
+ [文档] 2026-07-20：仓初始化（README/HANDOFF/plan、docs/00·13·17·18、fixtures 约定、deploy 测试 compose 样例）。两波：MVP 生文/生图 → Panda `:8013` → 全量含 RCA；永久非目标：注册机与 FlareSolverr。
