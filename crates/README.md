# crates/

后续 MVP 再填：

- `protocol` — body/headers/SSE 契约（对照 fixtures）
- `helper_client` — 调本机 curl_cffi helper
- `gateway` — axum 最小 `/v1/chat/completions` + `/v1/images/*`

当前无 Cargo 工程；未过文档门禁前不开写。
